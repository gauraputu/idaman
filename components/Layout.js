import Head from 'next/head';

const Layout = (props) => (
        <Head>
            <title>iDaman</title>
            <link rel="icon" href="/favicon.ico" />
        </Head>
);

export default Layout;